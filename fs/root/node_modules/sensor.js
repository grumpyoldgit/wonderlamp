var smoother = require('smoother.js');

function createSensorObject() {
	var sensor = [];
	sensor['smoother'] = smoother.newSmoother();
	sensor['last_value'] = 0;
}

module.exports = {
	newSensor: function() {
		return createSensorObject();
	}
}