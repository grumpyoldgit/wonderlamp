
//
// A smoother acts as a ring buffer, taking input values and returning averages.
//

// default buffer size

function Smoother(n) {
  this.values = new Array(n);
  for (var i=0; i<n; i++) {
    this.values[i] = 0;
  }
  this.index = 0;
}

Smoother.prototype.pushInput = function(v) {
  if (this.index >= this.values.length) {
    this.index = 0;
  } else {
    this.index++;
  }
  this.values[this.index] = v;
}

Smoother.prototype.average = function() {
  var result = 0.0;

  for (var i=0; i<this.values.length; i++) { // add all the values
    result += this.values[i];
  }
  result /= this.values.length; // divide by the length

  return Math.round(result);  
}


module.exports = {
  createSmoother: function(size) {
    return new Smoother(size);
  }
}