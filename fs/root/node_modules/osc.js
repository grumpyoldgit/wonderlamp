var osc = require('node-osc');
var client;

module.exports = {
	connect: function(host, port) {
		client = new osc.Client(host, port);
	}
	transmitFaderData: function (faderNumber, floatValue) {
		client.send('/1/fader' + String(faderNumber), floatValue);
	}
}